---
apiVersion: "networking.k8s.io/v1beta1"
kind: "Ingress"
metadata:
  name: "mimir"
  namespace: "abuse-api-dev"
spec:
  tls:
    - hosts:
      - "mimir.int.dev-godaddy.com"
      secretName: tls-mimir
    - hosts:
      - "mimir.cset.int.dev-gdcorp.tools"
      secretName: cset-wildcard
  rules:
  - host: "mimir.int.dev-godaddy.com"
    http:
      paths:
      - path: /
        backend:
          serviceName: "mimir"
          servicePort: 5000
  - host: "mimir.cset.int.dev-gdcorp.tools"
    http:
      paths:
      - path: /
        backend:
          serviceName: "mimir"
          servicePort: 5000
